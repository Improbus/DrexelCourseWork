package dragonchess.game.commands;

import junit.framework.Test;
import junit.framework.TestSuite;

/**
 * CommandsTestSuite
 *
 * @author Group 4
 */
public class CommandsTestSuite {

	/**
	 * Create the CommandsTestSuite.
	 */
	public static Test suite() {
		TestSuite suite = new TestSuite(
			"Test the dragonchess.game.commands package.");

		// add all the Tests to this TestSuite
//		suite.addTestSuite(MoveCommandTest.class);
//		suite.addTestSuite(PromoteCommandTest.class);
//		suite.addTestSuite(SetTurnCommandTest.class);

		return suite;
	}

	/**
	 * Runs the test suite using the textual runner.
	 */
	public static void main(String[] args) {
		junit.textui.TestRunner.run(CommandsTestSuite.suite());

		return;
	}

}
